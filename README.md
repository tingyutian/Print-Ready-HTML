# Print-Ready-HTML
A drop-in CSS skill for printing HTML pages cleanly — preserves background colors and layouts, removes browser-injected headers/footers, and prevents content from overflowing onto a second page. Defaults to US Letter portrait.
